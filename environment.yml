name: proteinlens
channels:
  - conda-forge
dependencies:
  - python=3.12
  - quarto
  - pip
  - pip:
      - pandas
      - numpy
      - matplotlib
      - plotly
      - scipy
      - statsmodels
      - jupyter
      - gradio
      - biopython
      - python-dotenv
      - openai
